namespace hblog {

    var WebPath: String;

    export class App {
        public static main(): number {
            console.log('Now running; web path is: ' + WebPath);
            return 0;
        }
    }

}